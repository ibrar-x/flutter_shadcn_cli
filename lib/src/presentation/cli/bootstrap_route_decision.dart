class BootstrapRouteDecision {
  final bool routeInitToMultiRegistry;
  final bool routeAddToMultiRegistry;

  const BootstrapRouteDecision({
    required this.routeInitToMultiRegistry,
    required this.routeAddToMultiRegistry,
  });
}
