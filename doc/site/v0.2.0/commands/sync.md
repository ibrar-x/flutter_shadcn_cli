# sync

## Purpose
Apply config changes to an existing install.

## Syntax

```bash
flutter_shadcn sync
flutter_shadcn sync @<namespace>
```

## Behavior Details

- Moves install/shared paths if changed
- Reapplies theme if theme id changed
- Refreshes manifests
